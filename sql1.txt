SELECT * FROM patients WHERE id = 234;

SELECT 
    p.*,
    (SELECT COUNT(*) FROM orders WHERE patient_id = p.id) as orders_count
FROM patients p 
WHERE p.id = 1;

-- Поиск пациента
SELECT * FROM patients 
WHERE full_name LIKE '%Иванов%' OR phone LIKE '%123%';